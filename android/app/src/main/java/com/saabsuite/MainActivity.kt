package com.saabsuite

import android.app.Activity
import android.os.Bundle

// Placeholder launcher activity satisfying AndroidManifest's MainActivity reference;
// wire in real UI per android/README.md when building this out into a full app.
class MainActivity : Activity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
